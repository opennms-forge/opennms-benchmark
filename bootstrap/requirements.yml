---
collections:
  - name: prometheus.prometheus
  # NOTE: versions prior to 0.21.0 use `when: (_common_dependencies)` with a string value,
  # which is rejected by Ansible 2.20+. Upgrade with:
  #   ansible-galaxy collection install prometheus.prometheus --upgrade
  # If the latest version still fails, set ALLOW_BROKEN_CONDITIONALS = True in ansible.cfg
  # until the upstream collection is patched.

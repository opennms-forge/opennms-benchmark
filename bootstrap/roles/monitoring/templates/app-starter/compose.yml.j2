---
services:

  app-starter:
    image: ghcr.io/indigo423/app-starter:latest
    deploy:
      resources:
        limits:
          cpus: 0.5
          memory: 128m
    environment:
      TZ: UTC
      MONITORING_IP: "{{ ansible_host }}"
    ports:
      - "3010:3000/tcp"
